<Application x:Class="Kloser.Desktop.Shell.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             StartupUri="MainWindow.xaml">
    <Application.Resources>
        <!--
            Phase 9 Step 4 — global brushes + control styles.
            Plan §4.1 calls for a "dense SaaS tool surface, no hero page".
        -->
        <SolidColorBrush x:Key="SurfaceBackgroundBrush" Color="#0F172A" />
        <SolidColorBrush x:Key="PanelBackgroundBrush"   Color="#1E293B" />
        <SolidColorBrush x:Key="PanelBorderBrush"       Color="#334155" />
        <SolidColorBrush x:Key="PrimaryTextBrush"       Color="#F8FAFC" />
        <SolidColorBrush x:Key="MutedTextBrush"         Color="#94A3B8" />
        <SolidColorBrush x:Key="AccentBrush"            Color="#38BDF8" />
        <SolidColorBrush x:Key="OkBrush"                Color="#22C55E" />
        <SolidColorBrush x:Key="WarnBrush"              Color="#F59E0B" />
        <SolidColorBrush x:Key="ErrorBrush"             Color="#EF4444" />
        <SolidColorBrush x:Key="SilentBrush"            Color="#64748B" />

        <Style TargetType="TextBlock" x:Key="HeaderText">
            <Setter Property="Foreground" Value="{StaticResource PrimaryTextBrush}" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="FontWeight" Value="SemiBold" />
        </Style>
        <Style TargetType="TextBlock" x:Key="LabelText">
            <Setter Property="Foreground" Value="{StaticResource MutedTextBrush}" />
            <Setter Property="FontSize" Value="11" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="TextAlignment" Value="Left" />
        </Style>
        <Style TargetType="TextBlock" x:Key="ValueText">
            <Setter Property="Foreground" Value="{StaticResource PrimaryTextBrush}" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="FontFamily" Value="Consolas" />
        </Style>
        <Style TargetType="Border" x:Key="PanelBorder">
            <Setter Property="Background" Value="{StaticResource PanelBackgroundBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource PanelBorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="6" />
            <Setter Property="Padding" Value="12" />
        </Style>
    </Application.Resources>
</Application>
